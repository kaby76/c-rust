# Rust application that implements a parser for the Antlr grammar for C.

* To build: `make`. You will need to adjust the .jar file for the Antlr Tool in build.sh.
* To run: `bash run.sh examples/add.c`.
